<!DOCTYPE html>
<html lang="zh-cn">
  <% include header %>

    <div class="container theme-showcase" role="main">
      <!-- Main jumbotron for a primary marketing message or call to action -->
      <div class="jumbotron">
        <h1><%= title %></h1>
        <p>This is a template showcasing the optional theme stylesheet included in Bootstrap. Use it as a starting point to create something more unique by building on or modifying it.</p>
      </div>
      <div class="row">
        <!-- Encoding/Encrypt form -->
        <a name='Encoding' id='Encoding'></a>
        <div class="col-sm-8">
          <form name="enForm" id="enForm" action="/Encoding" method="post">
            <div class="panel panel-success">
              <div class="panel-heading">
                <h3 class="panel-title">Encoding & Encrypt</h3>
              </div>
              <div class="panel-body">
                	Input your text:
  		  		    <p></p>
  		  		    <textarea name="encodeString" class="form-control" style='height:300px;' placeholder="" aria-describedby="sizing-addon3"><%= encodeString %></textarea>
  		  		    <p></p>
        		  	<div class="btn-group" role="group" aria-label="...">
        				  <button type="reset" class="btn btn-primary">Reset</button>
        				  <button type="submit" class="btn btn-success">Submit</button>
    				    </div>
              </div>
            </div>
          </form>
        </div>
      </div>

      <!-- Decoding form -->
      <div class="row">
        <a name='Decoding' id='Decoding'></a>
        <div class="col-sm-8">
          <form name="deForm" id="deForm" action="/Decoding" method="post" enctype="multipart/form-data">
            <div class="panel panel-info">
              <div class="panel-heading">
                <h3 class="panel-title">Decrypt & Decoding</h3>
              </div>
              <div class="panel-body">
                Select your Encoded File:
                <input name="encodeFile" type="file" class="form-control" placeholder="Username" aria-describedby="sizing-addon3">
                <p></p>
                <div class="btn-group" role="group" aria-label="...">
                  <button type="submit" class="btn btn-success">Submit</button>
                </div>
              </div>
            </div>
          </form>
        </div>
      </div>
    </div>
    <% include footer %>
  </body>
</html>